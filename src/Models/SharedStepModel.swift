//
// SharedStepModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct SharedStepModel: Codable, JSONEncodable, Hashable {

    internal var versionId: UUID
    internal var globalId: Int64
    internal var name: String
    @available(*, deprecated, message: "This property is deprecated.")
    internal var steps: [StepModel]
    internal var isDeleted: Bool

    internal init(versionId: UUID, globalId: Int64, name: String, steps: [StepModel], isDeleted: Bool) {
        self.versionId = versionId
        self.globalId = globalId
        self.name = name
        self.steps = steps
        self.isDeleted = isDeleted
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case versionId
        case globalId
        case name
        case steps
        case isDeleted
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(versionId, forKey: .versionId)
        try container.encode(globalId, forKey: .globalId)
        try container.encode(name, forKey: .name)
        try container.encode(steps, forKey: .steps)
        try container.encode(isDeleted, forKey: .isDeleted)
    }
}

