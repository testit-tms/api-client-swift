//
// TestSuiteV2PutModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct TestSuiteV2PutModel: Codable, JSONEncodable, Hashable {

    static let nameRule = StringRule(minLength: 0, maxLength: 255, pattern: nil)
    internal var id: UUID
    internal var parentId: UUID?
    internal var name: String
    internal var isDeleted: Bool
    internal var autoRefresh: Bool?

    internal init(id: UUID, parentId: UUID? = nil, name: String, isDeleted: Bool, autoRefresh: Bool? = nil) {
        self.id = id
        self.parentId = parentId
        self.name = name
        self.isDeleted = isDeleted
        self.autoRefresh = autoRefresh
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case parentId
        case name
        case isDeleted
        case autoRefresh
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(parentId, forKey: .parentId)
        try container.encode(name, forKey: .name)
        try container.encode(isDeleted, forKey: .isDeleted)
        try container.encodeIfPresent(autoRefresh, forKey: .autoRefresh)
    }
}

