//
// SectionMoveModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct SectionMoveModel: Codable, JSONEncodable, Hashable {

    /** Unique ID of the section */
    internal var id: UUID
    /** Unique ID of the section's current parent section */
    internal var oldParentId: UUID
    /** Unique ID of the section's target parent section */
    internal var parentId: UUID
    /** Unique ID of the section's following section */
    internal var nextSectionId: UUID?

    internal init(id: UUID, oldParentId: UUID, parentId: UUID, nextSectionId: UUID? = nil) {
        self.id = id
        self.oldParentId = oldParentId
        self.parentId = parentId
        self.nextSectionId = nextSectionId
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case oldParentId
        case parentId
        case nextSectionId
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(oldParentId, forKey: .oldParentId)
        try container.encode(parentId, forKey: .parentId)
        try container.encodeIfPresent(nextSectionId, forKey: .nextSectionId)
    }
}

