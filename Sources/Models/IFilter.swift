//
// IFilter.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public enum IFilter: Codable, JSONEncodable, Hashable {
    case typeCollectionFilter(CollectionFilter)
    case typeCompositeFilter(CompositeFilter)
    case typeFilter(Filter)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typeCollectionFilter(let value):
            try container.encode(value)
        case .typeCompositeFilter(let value):
            try container.encode(value)
        case .typeFilter(let value):
            try container.encode(value)
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode(CollectionFilter.self) {
            self = .typeCollectionFilter(value)
        } else if let value = try? container.decode(CompositeFilter.self) {
            self = .typeCompositeFilter(value)
        } else if let value = try? container.decode(Filter.self) {
            self = .typeFilter(value)
        } else {
            throw DecodingError.typeMismatch(Self.Type.self, .init(codingPath: decoder.codingPath, debugDescription: "Unable to decode instance of IFilter"))
        }
    }
}

