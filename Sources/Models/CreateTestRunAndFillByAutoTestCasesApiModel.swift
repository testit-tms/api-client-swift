//
// CreateTestRunAndFillByAutoTestCasesApiModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateTestRunAndFillByAutoTestCasesApiModel: Codable, JSONEncodable, Hashable {

    static let configurationIdsRule = ArrayRule(minItems: 1, maxItems: nil, uniqueItems: false)
    /** Specifies the GUID of the project, in which a test run will be created. */
    public var projectId: UUID
    /** Specifies the filter for selecting autotests, from which test points are created. */
    public var filter: CompositeFilter?
    /** Specifies the name of the test run. */
    public var name: String?
    /** Specifies the configuration GUIDs, from which test points are created. You can specify several GUIDs. */
    public var configurationIds: [UUID]
    /** Specifies the test run description. */
    public var description: String?
    /** Specifies the test run launch source. */
    public var launchSource: String?
    /** Specifies the test run launch options. */
    public var option: TestRunLaunchOptionApiModel
    /** Collection of tags to assign to the test run */
    public var tags: [String]?

    public init(projectId: UUID, filter: CompositeFilter? = nil, name: String? = nil, configurationIds: [UUID], description: String? = nil, launchSource: String? = nil, option: TestRunLaunchOptionApiModel, tags: [String]? = nil) {
        self.projectId = projectId
        self.filter = filter
        self.name = name
        self.configurationIds = configurationIds
        self.description = description
        self.launchSource = launchSource
        self.option = option
        self.tags = tags
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case projectId
        case filter
        case name
        case configurationIds
        case description
        case launchSource
        case option
        case tags
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(projectId, forKey: .projectId)
        try container.encodeIfPresent(filter, forKey: .filter)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encode(configurationIds, forKey: .configurationIds)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(launchSource, forKey: .launchSource)
        try container.encode(option, forKey: .option)
        try container.encodeIfPresent(tags, forKey: .tags)
    }
}

