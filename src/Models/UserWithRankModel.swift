//
// UserWithRankModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct UserWithRankModel: Codable, JSONEncodable, Hashable {

    internal var id: UUID
    internal var firstName: String
    internal var lastName: String
    internal var middleName: String
    internal var userName: String
    internal var displayName: String
    internal var userType: String
    internal var avatarUrl: String
    internal var avatarMetadata: String
    internal var isDeleted: Bool
    internal var isDisabled: Bool
    internal var providerId: UUID?
    internal var isActiveStatusByEntity: Bool
    internal var userRank: UserRankModel

    internal init(id: UUID, firstName: String, lastName: String, middleName: String, userName: String, displayName: String, userType: String, avatarUrl: String, avatarMetadata: String, isDeleted: Bool, isDisabled: Bool, providerId: UUID? = nil, isActiveStatusByEntity: Bool, userRank: UserRankModel) {
        self.id = id
        self.firstName = firstName
        self.lastName = lastName
        self.middleName = middleName
        self.userName = userName
        self.displayName = displayName
        self.userType = userType
        self.avatarUrl = avatarUrl
        self.avatarMetadata = avatarMetadata
        self.isDeleted = isDeleted
        self.isDisabled = isDisabled
        self.providerId = providerId
        self.isActiveStatusByEntity = isActiveStatusByEntity
        self.userRank = userRank
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case firstName
        case lastName
        case middleName
        case userName
        case displayName
        case userType
        case avatarUrl
        case avatarMetadata
        case isDeleted
        case isDisabled
        case providerId
        case isActiveStatusByEntity
        case userRank
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(firstName, forKey: .firstName)
        try container.encode(lastName, forKey: .lastName)
        try container.encode(middleName, forKey: .middleName)
        try container.encode(userName, forKey: .userName)
        try container.encode(displayName, forKey: .displayName)
        try container.encode(userType, forKey: .userType)
        try container.encode(avatarUrl, forKey: .avatarUrl)
        try container.encode(avatarMetadata, forKey: .avatarMetadata)
        try container.encode(isDeleted, forKey: .isDeleted)
        try container.encode(isDisabled, forKey: .isDisabled)
        try container.encodeIfPresent(providerId, forKey: .providerId)
        try container.encode(isActiveStatusByEntity, forKey: .isActiveStatusByEntity)
        try container.encode(userRank, forKey: .userRank)
    }
}

