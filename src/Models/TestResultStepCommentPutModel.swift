//
// TestResultStepCommentPutModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct TestResultStepCommentPutModel: Codable, JSONEncodable, Hashable {

    internal var id: UUID
    internal var text: String
    internal var stepId: UUID
    internal var parentStepId: UUID?
    internal var attachments: [AttachmentPutModel]

    internal init(id: UUID, text: String, stepId: UUID, parentStepId: UUID? = nil, attachments: [AttachmentPutModel]) {
        self.id = id
        self.text = text
        self.stepId = stepId
        self.parentStepId = parentStepId
        self.attachments = attachments
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case text
        case stepId
        case parentStepId
        case attachments
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(text, forKey: .text)
        try container.encode(stepId, forKey: .stepId)
        try container.encodeIfPresent(parentStepId, forKey: .parentStepId)
        try container.encode(attachments, forKey: .attachments)
    }
}

