//
// ParameterGroupModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct ParameterGroupModel: Codable, JSONEncodable, Hashable {

    internal var name: String
    internal var values: [String: String]
    internal var parameterKeyId: UUID

    internal init(name: String, values: [String: String], parameterKeyId: UUID) {
        self.name = name
        self.values = values
        self.parameterKeyId = parameterKeyId
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case values
        case parameterKeyId
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(values, forKey: .values)
        try container.encode(parameterKeyId, forKey: .parameterKeyId)
    }
}

