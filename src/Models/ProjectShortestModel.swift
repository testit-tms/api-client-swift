//
// ProjectShortestModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct ProjectShortestModel: Codable, JSONEncodable, Hashable {

    /** Unique ID of project */
    internal var id: UUID
    /** Indicates whether the project is deleted */
    internal var isDeleted: Bool
    /** Global ID of project */
    internal var globalId: Int64
    /** Name of project */
    internal var name: String
    /** Type of the project */
    internal var type: ProjectTypeModel

    internal init(id: UUID, isDeleted: Bool, globalId: Int64, name: String, type: ProjectTypeModel) {
        self.id = id
        self.isDeleted = isDeleted
        self.globalId = globalId
        self.name = name
        self.type = type
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case isDeleted
        case globalId
        case name
        case type
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(isDeleted, forKey: .isDeleted)
        try container.encode(globalId, forKey: .globalId)
        try container.encode(name, forKey: .name)
        try container.encode(type, forKey: .type)
    }
}

