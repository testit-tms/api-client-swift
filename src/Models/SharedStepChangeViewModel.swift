//
// SharedStepChangeViewModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct SharedStepChangeViewModel: Codable, JSONEncodable, Hashable {

    internal var id: UUID
    internal var globalId: Int64
    internal var name: String
    internal var steps: [WorkItemStepChangeViewModel]

    internal init(id: UUID, globalId: Int64, name: String, steps: [WorkItemStepChangeViewModel]) {
        self.id = id
        self.globalId = globalId
        self.name = name
        self.steps = steps
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case globalId
        case name
        case steps
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(globalId, forKey: .globalId)
        try container.encode(name, forKey: .name)
        try container.encode(steps, forKey: .steps)
    }
}

