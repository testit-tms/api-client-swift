//
// LinkPostModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct LinkPostModel: Codable, JSONEncodable, Hashable {

    static let urlRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** Link name. */
    internal var title: String?
    /** Address can be specified without protocol, but necessarily with the domain. */
    internal var url: String
    /** Link description. */
    internal var description: String?
    /** Specifies the type of the link. */
    internal var type: LinkType?
    internal var hasInfo: Bool

    internal init(title: String? = nil, url: String, description: String? = nil, type: LinkType? = nil, hasInfo: Bool) {
        self.title = title
        self.url = url
        self.description = description
        self.type = type
        self.hasInfo = hasInfo
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case title
        case url
        case description
        case type
        case hasInfo
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(title, forKey: .title)
        try container.encode(url, forKey: .url)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encode(hasInfo, forKey: .hasInfo)
    }
}

