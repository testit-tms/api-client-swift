//
// AutoTestStepModel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

internal struct AutoTestStepModel: Codable, JSONEncodable, Hashable {

    static let titleRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** Step name. */
    internal var title: String
    /** Detailed step description. It appears when the step is unfolded. */
    internal var description: String?
    /** Includes a nested step inside another step. The maximum nesting level is 15. */
    internal var steps: [AutoTestStepModel]?

    internal init(title: String, description: String? = nil, steps: [AutoTestStepModel]? = nil) {
        self.title = title
        self.description = description
        self.steps = steps
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case title
        case description
        case steps
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(title, forKey: .title)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(steps, forKey: .steps)
    }
}

