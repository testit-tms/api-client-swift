# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Swift

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          swift build -v
          # for debugging:
          # set -o pipefail
          # output=$(swift build -v 2>&1) || {
          #   echo "$output" | grep -i "error\|failed\|fatal" >&2
          #   exit 1
          # }
    #- name: Run tests
    #  run: swift test -v
